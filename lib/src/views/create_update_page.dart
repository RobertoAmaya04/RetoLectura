//pagina disitnta de libro estadisticas donde se actualiza la información de un libro debe ser similidar a la para agregar libros

import 'package:flutter/material.dart';

class CreateUpdatePage extends StatelessWidget {
  const CreateUpdatePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
